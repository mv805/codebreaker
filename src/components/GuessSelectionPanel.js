import React from 'react';
import styles from './GuessSelectionPanel.module.css';

const GuessSelectionPanel = () => {
    return (
        <div className={ styles.panel }>
            <button></button>
        </div>
    );
};

export default GuessSelectionPanel;